# Copy to .env to override the defaults used by docker-compose.yml.
# These defaults work out of the box for local development.

# Database (shared by the database and backend services)
DB_NAME=task_manager
DB_USER_NAME=task_manager
DB_PASSWORD=task_manager

# Host ports the services are published on
BACKEND_PORT=4000
FRONTEND_PORT=3100
